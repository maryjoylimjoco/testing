<% if flash.any? %>
  <div class="flash-container">
    <% flash.each do |type, message| %>
      <div class="alert alert-#{type}" role="alert">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
